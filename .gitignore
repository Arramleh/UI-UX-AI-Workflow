# Workflow output — one folder per feature (reports/notification-center/, ...).
# Local configuration (Figma token, PRD source, feature slug)
.env

node_modules/

# Claude Code worktrees — transient checkouts, never part of this project
.claude/worktrees/
